# Copie ce fichier en ".env.build" (meme dossier) et remplis avec
# l'IP/domaine reel de ton VPS avant de lancer scripts/build_release.sh.
# ".env.build" est ignore par Git (voir .gitignore) - chaque deploiement
# peut avoir sa propre valeur sans jamais toucher au code Dart.

API_BASE_URL=http://74.208.224.9:3000/api
WS_URL=ws://74.208.224.9:3000/ws
